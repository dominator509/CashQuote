# CashQuote 100-Pass Hardening Log

Started: 2026-06-27

Scope: 100 thorough end-to-end hardening/debugging/security/functionality passes across the full repository. Each pass should inspect a distinct risk angle, fix findings before moving on where practical, and record verification evidence.

## Pass Ledger

| Pass | Focus | Findings | Fixes | Verification |
| --- | --- | --- | --- | --- |
| 001 | Baseline repo hygiene, static scan, dependency audit, lint/type/build/test entrypoints | Found pilot tenant provisioning collision: users with the same email local-part on different domains could be attached as co-owners to the same globally name-matched business. | Removed global business-name reuse during owner membership provisioning; added a security regression test for same-generated-name pilot users. | `npm audit` 0 vulns; `npm run lint` pass; `npm run typecheck` pass; `npm run build` pass; `npm run test:unit` 23 pass; `npm run test:integration` 1 pass; `npm run test:security` 10 pass; root `npm test -- --runInBand` 34 pass. |
| 002 | Runtime/API negative-path review for tenant context | Found `x-business-id` was cast directly from Express headers; blank values and duplicated/array headers were not rejected before membership lookup. | Added strict tenant header normalization in `requireBusinessId`; blank or non-string values now fail with `400 TENANT_REQUIRED`; added unit regressions. | `npm run test:unit` 25 pass; `npm run typecheck` pass. |
| 003 | Financial mutation and payment state-machine review | Found invoice updates could lower an invoice total below already-recorded payments, creating an overpaid state that payment creation would otherwise reject. | `updateInvoice` now includes payments and rejects totals below the recorded paid total; added focused unit regression coverage. | `npm run test:unit` 26 pass; `npm run typecheck` pass. |
| 004 | Quote conversion/idempotency duplicate-request review | Found redundant controller-level conversion activity logging after the service already logs inside the transaction; a post-commit log failure could make a successful conversion return 500. Also lacked explicit duplicate-constraint race coverage. | Removed redundant post-commit `quote_convert` log from the controller; added P2002 duplicate conversion race regression returning stable 409. | `npm run test:unit` 27 pass with conversion service 100% coverage; `npm run typecheck` pass. |
| 005 | Radar/revenue insight correctness review | Found Lost Cash Radar used activity logs as the conversion source of truth, so missing/legacy logs could show already-converted quotes as unconverted revenue at risk. | Changed radar conversion detection to query invoices by authoritative `sourceQuoteId`; updated unit coverage to assert this query path. | `npm run test:unit` 27 pass; `npm run typecheck` pass. |
| 006 | Production runtime/static serving/health readiness review | Found a leftover `/api/test-protected` debug route and no explicit production SPA build existence guard before static fallback. | Removed the debug route; added `getStaticClientBuild` guard so production startup fails closed when `client/dist/index.html` is missing; added security regressions for both. | `npm run test:security` 12 pass; `npm run typecheck` pass. |
| 007 | Production error response and stack-trace hardening review | Found validation errors always returned Zod issue details, including in production, and unhandled errors used raw `console.error` instead of the structured request logger. | Production validation responses now return only stable `{ error, code }`; non-production keeps details; unhandled errors log through request logger/pino fallback. | `npm run test:security` 13 pass; `npm run typecheck` pass. |
| 008 | Auth/session/cookie lifecycle review | Found signed JWTs were trusted via direct cast, so a token without a usable `userId` could pass signature verification and fail later as missing context instead of being rejected as invalid auth. | `requireAuth` now validates decoded JWT payload shape and rejects missing/blank `userId` as `AUTH_INVALID`; added request-level regression. | `npm run test:security` 14 pass; `npm run typecheck` pass. |
| 009 | Mail/reminder provider and state transition review | Found reminder status updates and activity logs were not atomic, and partial SMTP configuration could fall through to mock email when mock was allowed. | Reminder create/send/resolve now write reminder state plus activity log in Prisma transactions; partial SMTP config now fails with `EMAIL_NOT_CONFIGURED`; added unit regressions. | `npm run test:unit` 29 pass; `npm run typecheck` pass. |
| 010 | AI provider/fallback telemetry and schema hardening review | Found AI generation accepted unbounded notes before provider/DB persistence and OpenAI adapter failures used raw `console.error`. | Added 5000-character AI notes cap with integration regression; changed OpenAI adapter failure logging to structured pino logger. | `npm run test:integration` 2 pass; `npm run typecheck` pass. |
| 011 | Client/frontend auth and workflow resilience review | Found protected API `401/403` responses left stale workspace state visible, and the AI generation API had no frontend surface despite the AI-assisted quote workflow boundary. | Added shared client workspace clearing on protected auth failures/logout; added quote-builder AI draft generation that fills the existing line-item form; added Playwright coverage for the AI draft path. | `npm run typecheck` pass; `npm run lint` pass; `npm run build` pass; temp PostgreSQL migration deploy pass; `npm run test:e2e` 2 pass. |
| 012 | Owner-only activity log pagination review | Found `GET /api/activity-logs` used the extra unseen row as `nextCursor`; because the next page also uses `skip: 1`, that unseen row was permanently skipped. | Changed pagination to return the last visible row as `nextCursor`; added a unit regression proving the next page starts after the last returned log. | `npx jest --runInBand tests/unit/activity_pagination.test.ts` pass; `npm run typecheck` pass; `npm run test:unit` 30 pass. |
| 013 | Financial discount integrity review | Found quote and invoice create/update paths allowed `discountAmount` greater than subtotal, storing contradictory records such as positive subtotal, larger discount, and zero total. | Added an explicit discount-within-subtotal invariant and enforced it across quote/invoice creation and updates with stable `DISCOUNT_EXCEEDS_SUBTOTAL` errors; added controller regressions for all four mutation paths. | `npx jest --runInBand tests/unit/financial_discount_integrity.test.ts` 4 pass; `npm run typecheck` pass; `npm run test:unit` 34 pass; `npm run lint` pass. |
| 014 | Request body parser and production error boundary review | Found Express JSON parser failures for malformed or oversized bodies were treated as generic internal errors, losing stable client error codes and polluting internal error telemetry. | Added explicit body-parser error handling for `MALFORMED_JSON` and `PAYLOAD_TOO_LARGE`; added production security regressions using raw malformed and oversized JSON requests. | `npm run test:security` 15 pass; `npm run typecheck` pass; `npm run lint` pass. |
| 015 | Invoice paid-status integrity review | Found invoice create/update accepted direct `paid` status even when no recorded payments covered the invoice total, and updates could raise a paid invoice total above payments while leaving it paid. | Invoice mutations now derive `paid/unpaid` from recorded payment coverage while preserving explicit `void`; added regressions for paid-status creation and total-raise updates. | `npx jest --runInBand tests/unit/invoice_financial_state.test.ts` 3 pass; `npm run typecheck` pass; `npm run test:unit` 36 pass; `npm run lint` pass. |
| 016 | Client deletion and financial record retention review | Found owner-only client deletion cascaded through Prisma relations into quotes, invoices, invoice line items, and payments, allowing a routine client delete to destroy financial history. | Client deletion now rejects clients that have quotes or invoices with stable `CLIENT_HAS_FINANCIAL_RECORDS`; added regressions for blocked and allowed delete paths. | `npx jest --runInBand tests/unit/client_delete_integrity.test.ts` 2 pass; `npm run typecheck` pass; `npm run test:unit` 38 pass; `npm run lint` pass. |
| 017 | Converted quote deletion and invoice lineage review | Found deleting a quote after conversion would `SetNull` invoice `sourceQuoteId`, breaking conversion idempotency and Lost Cash Radar's authoritative converted-quote detection. | Quote deletion now rejects converted quotes with stable `QUOTE_HAS_INVOICE`; added regressions for blocked converted quote deletion and allowed unconverted quote deletion. | `npx jest --runInBand tests/unit/quote_delete_integrity.test.ts` 2 pass; `npm run typecheck` pass; `npm run test:unit` 40 pass; `npm run lint` pass. |
| 018 | Invoice deletion and payment-history retention review | Found owner-only invoice deletion cascaded into recorded payments, allowing a routine invoice delete to erase payment history. | Invoice deletion now rejects invoices with recorded payments using stable `INVOICE_HAS_PAYMENTS`; added regressions for blocked paid-history deletion and allowed no-payment deletion. | `npx jest --runInBand tests/unit/invoice_delete_integrity.test.ts` 2 pass; `npm run typecheck` pass; `npm run test:unit` 42 pass; `npm run lint` pass. |
| 019 | Active reminder referential integrity review | Found quote and invoice deletion could leave pending/sent reminders pointing at missing financial records because reminders use polymorphic IDs without database FKs. | Quote and invoice deletion now reject active reminders with stable `QUOTE_HAS_ACTIVE_REMINDERS` / `INVOICE_HAS_ACTIVE_REMINDERS`; added regressions for both entity types. | `npx jest --runInBand tests/unit/quote_delete_integrity.test.ts tests/unit/invoice_delete_integrity.test.ts` 6 pass; `npm run typecheck` pass; `npm run test:unit` 44 pass; `npm run lint` pass. |
| 020 | Reminder send target revalidation review | Found `sendReminder` trusted the reminder's polymorphic `entityId` and could email/update stale reminders whose quote or invoice target no longer exists. | Reminder sending now revalidates the target quote/invoice before mail dispatch or status updates; added a regression that stale targets fail closed without updating reminder status. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 22 pass; `npm run typecheck` pass; `npm run test:unit` 45 pass; `npm run lint` pass. |
| 021 | Duplicate active reminder review | Found reminder creation allowed multiple pending/sent reminders for the same quote or invoice, enabling repeated operational follow-ups for the same entity. | Reminder creation now rejects duplicate active reminders with stable `ACTIVE_REMINDER_EXISTS` while allowing resolved reminders to remain historical; added duplicate-active regression coverage. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 23 pass; `npm run typecheck` pass; `npm run test:unit` 46 pass; `npm run lint` pass. |
| 022 | Void invoice payment deletion review | Found payment deletion did not mirror payment creation's void-invoice guard, so deleting a payment from a void invoice could revive it as unpaid/paid and mutate historical payment state. | `deleteInvoicePayment` now rejects void invoices with `Cannot delete payments for a void invoice`; added regression coverage that no payment delete or invoice status update occurs. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 24 pass; `npm run typecheck` pass; `npm run test:unit` 47 pass; `npm run lint` pass. |
| 023 | Future-dated payment state review | Found payment creation accepted future `paidAt` values and immediately counted them toward invoice paid status, allowing invoices to be marked paid before payment occurred. | `createInvoicePayment` now rejects future payment dates with stable `PAYMENT_DATE_IN_FUTURE` before reading or writing invoice/payment state; added regression coverage. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 25 pass; `npm run typecheck` pass; `npm run test:unit` 48 pass; `npm run lint` pass. |
| 024 | Backdated reminder scheduling review | Found reminder creation accepted stale past `scheduledAt` values, allowing already-expired operational reminders to be stored and later sent as if newly scheduled. | `createReminder` now rejects clearly past schedules with stable `REMINDER_DATE_IN_PAST` while allowing near-now clock skew; added regression coverage that stale reminders fail before target lookup or writes. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 26 pass; `npm run typecheck` pass; `npm run test:unit` 49 pass; `npm run lint` pass. |
| 025 | Converted quote mutation review | Found converted quotes were protected from deletion but not from updates, so the source quote's status, totals, or line items could be rewritten after generating an invoice. | `updateQuote` now counts `sourceInvoices` and rejects converted quote updates with stable `QUOTE_HAS_INVOICE`; added regression coverage that no transaction, line-item rewrite, quote update, or activity log occurs. | `npx jest --runInBand tests/unit/quote_update_integrity.test.ts` 1 pass; `npm run typecheck` pass; `npm run test:unit` 50 pass; `npm run lint` pass. |
| 026 | Void invoice mutation review | Found void invoices could still be updated; omitting `status: "void"` let status derivation revive them as unpaid/paid and line-item updates could rewrite terminal financial history. | `updateInvoice` now rejects existing void invoices with stable `INVOICE_IS_VOID` before starting a transaction; added regression coverage that no line-item rewrite, invoice update, or activity log occurs. | `npx jest --runInBand tests/unit/invoice_financial_state.test.ts` 4 pass; `npm run typecheck` pass; `npm run test:unit` 51 pass; `npm run lint` pass. |
| 027 | Activity log pagination stability review | Found owner audit-log pagination ordered only by `createdAt`, so logs created in the same timestamp window could page nondeterministically and duplicate or skip support evidence. | `listActivityLogs` now orders by `createdAt desc` plus `id desc`; updated pagination coverage and added a deterministic same-timestamp ordering regression. | `npx jest --runInBand tests/unit/activity_pagination.test.ts` 2 pass; `npm run typecheck` pass; `npm run test:unit` 52 pass; `npm run lint` pass. |
| 028 | Production JWT secret strength review | Found production rejected a missing `JWT_SECRET` but still accepted the repository's known `development-only-jwt-secret` if it was explicitly configured, allowing a private-pilot deploy with a public default signing key. | `getJwtSecret` now rejects the development default in production with stable `CONFIG_WEAK_SECRET`, and readiness validation uses the same accessor; added unit and `/readyz` security regressions. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 27 pass; `npx jest --runInBand tests/security/private_pilot_hardening.test.ts` 15 pass; `npm run typecheck` pass; `npm run test:unit` 53 pass; `npm run test:security` 16 pass; `npm run lint` pass. |
| 029 | Production pilot access-code strength review | Found production required `PILOT_ACCESS_CODE` but accepted short/default values such as `pilot-code`, making private-pilot access guessable if a placeholder reached deployment. | Added `getPilotAccessCode` with production-only minimum length and known-placeholder rejection; `pilotLogin` and readiness now share it; updated docs and E2E defaults to use long private-code examples. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 28 pass; `npx jest --runInBand tests/security/private_pilot_hardening.test.ts` 17 pass; `npm run typecheck` pass; `npm run test:unit` 54 pass; `npm run test:security` 18 pass; `npm run lint` pass. |
| 030 | Production origin configuration review | Found production readiness accepted wildcard, malformed, or path-bearing `APP_ORIGIN` / `CORS_ORIGIN` values, so a deploy could pass `/readyz` while cross-origin auth was broken or unsafe. | Added production-only HTTP(S) origin validation for `APP_ORIGIN` and every `CORS_ORIGIN` entry with stable `CONFIG_INVALID_ORIGIN`; documented the launch requirement and added unit plus `/readyz` regressions. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 29 pass; `npx jest --runInBand tests/security/private_pilot_hardening.test.ts` 19 pass; `npm run typecheck` pass; `npm run test:unit` 55 pass; `npm run test:security` 20 pass; `npm run lint` pass. |
| 031 | Financial line-item validation review | Found quote/invoice create schemas defaulted missing line items to `[]` and update schemas accepted `lineItems: []`, allowing empty financial documents and zero-total invoices that could derive as paid. | Shared quote/invoice create schemas now require at least one line item, and update schemas reject explicit empty line-item replacements; added controller-boundary regressions proving no DB lookup or rewrite occurs. | `npx jest --runInBand tests/unit/financial_line_item_integrity.test.ts` 4 pass; `npm run typecheck` pass; `npm run test:unit` 59 pass; `npm run lint` pass. |
| 032 | Post-mutation activity logging resilience review | Found controllers awaited `logActivity` after successful data mutations, so an activity-log insert failure could return 500 even though the primary create/update/delete already committed. | `logActivity` is now best-effort: it logs structured telemetry on audit-write failure without throwing; transaction-local financial/reminder logs that use `tx.activityLog` remain atomic. | `npx jest --runInBand tests/unit/activity_logging_resilience.test.ts` 1 pass; `npm run typecheck` pass; `npm run test:unit` 60 pass; `npm run lint` pass. |
| 033 | Reminder due-time enforcement review | Found `sendReminder` ignored `scheduledAt`, so future-dated reminders could be sent immediately and marked sent before their intended schedule. | `sendReminder` now rejects reminders scheduled beyond clock skew with stable `REMINDER_NOT_DUE` before target lookup, mail dispatch, status update, or activity logging; added regression coverage. | `npx jest --runInBand tests/unit/auth_tenant_ai_payment_reminder.test.ts` 30 pass; `npm run typecheck` pass; `npm run test:unit` 61 pass; `npm run lint` pass. |
| 034 | Payment method input validation review | Found payment `method` accepted any non-empty string, including whitespace-padded labels and unbounded values that could bloat payment/audit records. | `createPaymentSchema` now trims method values, rejects blank-after-trim methods, and caps methods at 50 characters; added shared schema regressions. | `npx jest --runInBand tests/unit/payment_schema_integrity.test.ts` 3 pass; `npm run typecheck` pass; `npm run test:unit` 64 pass; `npm run lint` pass. |
| 035 | Client input normalization and bounds review | Found client names, billing addresses, and tags were not normalized or bounded, allowing whitespace-only names and unbounded tag/address values to flow into billing records. | Client schema now trims names/addresses/tags, rejects blank names and tags after trim, caps names at 120 chars, billing addresses at 1000 chars, tags at 50 chars each, and tag count at 20; added schema regressions. | `npx jest --runInBand tests/unit/client_schema_integrity.test.ts` 4 pass; `npm run typecheck` pass; `npm run test:unit` 68 pass; `npm run lint` pass. |
| 036 | Line item input bounds review | Found line item descriptions and categories were not normalized or bounded, and quantity/price lacked practical upper caps, allowing noisy labels or unrealistic financial values into quotes and invoices. | `lineItemSchema` now trims descriptions/categories, rejects blank-after-trim category/description values, caps description/category lengths, caps quantity at 10,000, and caps unit price at 100,000,000 cents; added schema regressions. | `npx jest --runInBand tests/unit/line_item_schema_integrity.test.ts` 3 pass; `npm run typecheck` pass; `npm run test:unit` 71 pass; `npm run lint` pass. |

## Operating Notes

- Preserve tenant isolation, production fail-closed behavior, Prisma migration safety, and private-pilot MVP boundaries.
- Do not modify user-local `.obsidian` state unless a pass explicitly targets Obsidian config.
- Generated/cache/dependency outputs are not source findings unless they affect reproducible validation.
- Keep evidence concise; link to command output summaries instead of pasting full logs.
